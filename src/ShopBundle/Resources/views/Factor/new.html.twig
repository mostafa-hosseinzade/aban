{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Factor creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('factor') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
