{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Factor list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Total</th>
                <th>Descr</th>
                <th>Refsale</th>
                <th>Ipclient</th>
                <th>Date</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('factor_show', { 'id': entity.id }) }}">{{ entity.total }}</a></td>
                <td>{{ entity.descr }}</td>
                <td>{{ entity.refsale }}</td>
                <td>{{ entity.ipClient }}</td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('factor_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('factor_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('factor_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
