{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Basket list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Number</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('basket_show', { 'id': entity.id }) }}">{{ entity.number }}</a></td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('basket_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('basket_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('basket_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
